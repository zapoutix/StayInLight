namespace StayInLight.Controllers
{
    public class BlockerEnemyController : EnemyControllerBase
    {
        protected override float BurningTimeInSec => 3f;


        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
